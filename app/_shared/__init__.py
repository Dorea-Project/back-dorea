"""Seedwork — briques tactiques DDD partagées par tous les contextes bornés.

Contient uniquement des abstractions génériques (Entity, AggregateRoot,
ValueObject, DomainEvent, Repository, DomainError). Aucun code métier, aucun
couplage framework dans la couche `domain`.
"""
